---
description: Save context using the remember skill
---

Use the `remember` skill for this request.

User input: `$ARGUMENTS`

If `User input` is empty, ask the user whether they want to save `memory`, `project`, or `scratch` context.
If `User input` is not empty, interpret it exactly as the remember skill expects.
