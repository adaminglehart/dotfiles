---
description: Recall context using the recall skill
---

Use the `recall` skill for this request.

User input: `$ARGUMENTS`

If `User input` is empty, perform light recall for the current project.
If `User input` is not empty, interpret it exactly as the recall skill expects.
